function solution(A, B, C) {
  // Implement your solution here
  Alen = A.length;

  let answer = 0;

  let l = 1;
  let r = C.length;

  while (l <= r) {
    let mid = Math.floor((l + r) / 2); //못의갯수
    let cur = 0; // 현재 사용한 못의 갯수
    while (cur < mid) {
      for (let i = 0; i < Alen; i++) {}
    }
  }
}
